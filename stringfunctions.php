<?php
    $username = "Vaishnavi hello world";
    $phone = "123-45-6-78";
    // $username = strtolower($username);
    // $username = strtoupper($username);
    // $username = trim($username);//to remove any whitespaes
    //$username = str_pad($username,20,'a');//to add characters by specifiying with limit
    // $phone= str_replace("-","",$phone);
    // $username = strrev($username);
    // $username = str_shuffle($username);
    // $username = strcmp($username,"Vaishnavi");//if it equals it return 0 and if doesnt equal equal it will return 1
    // $username = strlen($username);
    // $index = strpos($username,"i");//string position
    // $username = ucwords($username); //capitalize every first letter of the word

    #is_string()
    #check if string

    // $val = 'Hello';
    // if(is_string($val)){
    //     echo "It is String";
    // }else{
    //     echo "It is not String";
    // }

    // $values = [true,false,null,'abc',33,'33',22.4,'22.4','',0,'0'];
    // foreach($values as $value){
    //     if(is_string($value)){
    //         echo "$value is a string <br>";
    //     }
    // }

    // echo $username  ;

?>